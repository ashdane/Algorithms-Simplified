for(i=0; i<arr.length; i++)
{
    let min = arr[i];
    for(j=i; j<arr.length; j++)
    {
        if(arr[j]<min)
        {
            let temp = min;
            min = arr[j];
            arr[j] = temp;
        }
    }
    arr[i] = min;
}
